<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/login_background</item>
        <item name="colorPrimaryDark">@color/otrocolor</item>
        <item name="android:navigationBarColor">@color/otrocolor</item>
    </style>

    <style name="CircularProgress" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorAccent">@color/ProgressBar</item>
    </style>

</resources>
