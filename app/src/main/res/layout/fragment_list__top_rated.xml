<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent" android:nestedScrollingEnabled="true" android:orientation="vertical">
    <ProgressBar
            android:theme="@style/CircularProgress"
            android:layout_width="match_parent"
            android:id="@+id/progress_bar_toprated" android:padding="170dp"
            android:layout_marginBottom="731dp" android:layout_height="720dp"/>


    <ListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/lista"
            android:persistentDrawingCache="all"
            android:fastScrollAlwaysVisible="false"
            android:nestedScrollingEnabled="true"/>

</LinearLayout>